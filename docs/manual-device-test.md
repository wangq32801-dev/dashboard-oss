# 真机人工测试清单（折叠屏 Safari / Mac / PWA）

> 规则：**预期结果在测试前写死；实测结果由执行人如实填写**，两者不得互相抄袭。
> Chromium 触摸模拟（smoke 73 项）只证明响应式布局与交互逻辑，**不构成 Safari/WebKit 证据**。
> 执行环境：iOS 折叠屏（Safari 浏览器模式 + PWA 独立模式各一遍）、Mac Safari/Chrome。
> 本批执行状态：**未执行（无真机，环境阻塞）**——全部「实测」列留空，不得预填。

## 使用方法

线上入口 `https://<你的域名>/`（发布后）。每项记录：通过 / 失败（现象+截图）。任何 P1 失败停止后续项并回报。

## A. 折叠屏 Safari · 折叠态（412×915 级）

| # | 操作 | 预期结果 | 实测 |
|---|---|---|---|
| A1 | 冷启动 | 首帧即上次视图，无「先今日后跳转」闪跳 | |
| A2 | 首屏观察（今日） | 无需滚动可见：状态带/今日重点/≥1 条行动；无「加载中」滞留 | |
| A3 | 点「全局」 | 回到上次全局分区（非今日/复盘/管家干扰） | |
| A4 | 六分区横滑+点击 | 激活项自动滚入可视；无横向页面溢出 | |
| A5 | 全局态量导航高度 | 底栏+分区条 ≤120px；内容不被遮挡、底部无白洞 | |
| A6 | 一次轻滚 | 到习惯打卡；再轻滚到快速收集 | |
| A7 | 勾选一条今日行动 | 即时划掉；**下拉刷新后仍已完成**（权威回读） | |
| A8 | 快速收集输入一条 | 提示成功；刷新后收集箱存在该条 | |
| A9 | 神谕折叠行点击展开/收起 | 正文展开收起；不跳视图、不串健康页 | |
| A10 | 软键盘弹出（收集框） | 输入框不被键盘遮挡；收起后布局复原 | |
| A11 | 折叠→展开物理形变 | 当前视图保持；无残影/白屏；分区条仍在 | |
| A12 | 断网（飞行模式）操作 | 写入失败有明确提示且输入保留；恢复网络后人工重试成功 | |

## B. 折叠屏 Safari · 展开态（768×1024 级）

| # | 操作 | 预期结果 | 实测 |
|---|---|---|---|
| B1 | 展开态进全局 | 底栏 4 入口+分区条；高度 ≤120px；无横向溢出 | |
| B2 | 横竖屏切换 | 当前视图/滚动位置/草稿保持 | |
| B3 | 复盘页查看本周战报 | 战报卡出现在复盘且图表渲染（非空白/0 宽） | |

## C. Mac 浏览器

| # | 操作 | 预期结果 | 实测 |
|---|---|---|---|
| C1 | 侧栏全局 6 子项逐个点击 | 每个激活正确视图、子项高亮 | |
| C2 | ⌘K | 命令板打开；「全局·关系/健康/积极主动」3 条新命令可达正确视图 | |
| C3 | 侧栏 ⌘K 提示条点击 | 打开命令板（键盘 Tab 可达、Enter 可触发） | |
| C4 | 随行顾问 ✦ 打开→点 ↻ 重试/✕ 关闭/模式切换 | 三者均工作（事件委托改造后） | |
| C5 | 冷启动恢复健康页 | 侧栏「全局」+「健康」同时高亮 | |

## D. PWA 独立模式（添加到主屏幕）

| # | 操作 | 预期结果 | 实测 |
|---|---|---|---|
| D1 | 安装后首启 | 正常进入；无 SW 旧缓存导致的旧版页面 | |
| D2 | 发布新版后重进 | 页面版本哨兵 3s 自更新；**不循环刷新** | |
| D3 | 离线冷启动 | 外壳可用（缓存页）；联网后数据自动校准 | |
| D4 | 草稿保持 | 收集框/大石头草稿切走切回不丢 | |

## E. 网络与版本

| # | 操作 | 预期结果 | 实测 |
|---|---|---|---|
| E1 | 弱网（慢 3G 模拟或弱 Wi-Fi）进健康页 | 骨架/旧快照先显；无整页白屏 | |
| E2 | 双标签同时改同一任务 | 后写者收到冲突/幂等处理；无静默覆盖 | |
| E3 | `/api/ops/status` 回读 | healthy；release 与最近发布一致；rollback_available true | |

## 实测记录（执行人填写）

- 日期/设备/系统版本：
- 失败项与现象：
- 截图路径：
