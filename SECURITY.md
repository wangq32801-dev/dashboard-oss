# 安全须知

## 部署红线

**绝不要在无认证的情况下把本服务直接暴露到公网。** 它读写你的任务、笔记、关系记录，且 AI 动作可以创建/修改真实数据。后端默认只监听 `127.0.0.1`，请保持这一默认，除非你清楚自己在做什么。

公网访问的最低要求（全部自担风险）：

1. TLS 终端（反向代理或隧道自带）
2. 认证层（Basic Auth / Access 前置 / mTLS 之一）
3. 后端仍只绑回环，由代理转发

项目自带的纵深防御（不替代上述要求）：

- 非回环来源请求默认拒绝（`DASH_ALLOW_REMOTE=1` 可关闭，仅当你有可信反代时）
- 静态文件白名单 + 路径穿越（`..`、编码变体）拒绝
- 写操作幂等账本 + 权威回读 + 软删除（7 天可恢复）+ 恢复快照
- AI 动作白名单 + 人工确认 + 审计日志 + 撤销栈
- 错误响应脱敏（路径/查询串/凭据特征不回传客户端）

## 密钥管理

本地 HTTP 只接受回环 Host；反代需显式设置 `DASH_ALLOWED_HOSTS`（逗号分隔主机名）及 `DASH_ALLOWED_ORIGINS`（完整协议、主机和端口）。容器网桥场景使用 `DASH_ALLOW_REMOTE=1`，仅可配合回环发布端口和可信反代；不能因此省略认证。强制演示模式每次启动使用新建临时数据目录，并忽略单文件路径覆盖，退出后清理。

- 所有凭据放项目目录 `config` 或通过受控环境变量注入，权限建议 `chmod 600`
- **仓库不内置任何开发者密钥**；`deploy/.env.example` 只含空占位键名
- `TICKTICK_TOKEN`、`LLM_API_KEY`、`VISION_API_KEY`、`WECHAT_DELIVER` 均已被 `.gitignore` 覆盖的配置文件承载，不要写进代码或提交
- 万一提交了密钥：轮换该密钥（从服务方控制台），再用历史清理工具处理——仅删除文件不够

## 本地数据权限

后端对状态文件强制 owner-only（`0600`）写入。`data/` 目录含你的全部个人数据，备份/同步时按敏感文件对待。

## 漏洞报告

请勿对他人部署的实例进行测试。发现问题请开 GitHub Issue（敏感细节可要求维护者提供加密渠道），或直接提交修复的 PR。我们会在 72 小时内响应。

## 供应链

- 零 pip 运行时依赖；前端唯一第三方库为 Chart.js（MIT，随仓库分发以便离线）
- 浏览器端无外部 CDN 依赖（PWA 可完全离线运行）
