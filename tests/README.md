# 驾驶舱回归测试

折叠屏冒烟测试默认访问本机服务，并在 412×915（折叠）和 768×1024（展开）两个尺寸检查启动、健康页 7 天默认、横向溢出和静止后的 DOM 稳定性：

```bash
DASH_URL=http://127.0.0.1:8787 node tests/foldable-smoke.mjs
```

可用 `PLAYWRIGHT_EXECUTABLE` 指定浏览器路径。测试不读取凭据，也不会写入业务数据；线上测试需要显式提供已鉴权的 `DASH_URL`，并应在测试结束后核对服务日志。
