# 隐私说明（AI 能看到你的什么数据）

本项目是**本地优先**应用：你的任务、笔记、健康数据默认只存在于你自己的机器上（`data/` 目录）。**但当你启用 AI 功能时，一部分数据会发送给你自己配置的模型服务**。这份文档逐字段说清楚——不读代码也知道边界在哪。

## 一、总原则

1. **AI 完全可选**。不配置 `LLM_API_KEY`，所有 AI 功能自动降级为规则模式，界面完整可用。
2. **数据只发给你配置的服务**。项目不内置任何开发者的模型端点或密钥；`LLM_API_BASE` 指向哪里（OpenAI / DeepSeek / 火山方舟 / 本地 Ollama），数据就去哪里。
3. **常规功能不发任何数据**。浏览、打卡、任务增删改、复盘保存、关系记录——这些纯本地/TickTick 操作不经过 LLM。

## 二、AI 管家对话时，模型看到的字段

打开管家（或顾问）并发送第一条消息时，以下上下文会随对话一起发送（均为**结构化注入**，非原始文件）：

| 数据块 | 具体字段 | 截断限制 |
|---|---|---|
| 角色 | 每个角色的名称、使命（前 40 字）、关键结果标题（≤4 条）、关联习惯名+打卡数 | 使命 40 字 |
| 待办任务 | 标题（前 42 字）+ 标记（是否大石头/到期日/所属角色/优先级） | 最多 14 条 |
| 习惯 | 名称、维度、所属角色、连续天数、本周 n/7、今日是否打卡 | 全量（无截断） |
| 情感账户 | 对方名称、存款/取款、金额、事由（前 40 字）、日期 | 最多 8 条 |
| 倾听笔记 | 对方名称、感受/复述/第三选择（各前 24 字） | 最多 6 条 |
| 影响圈 | 关注圈/影响圈标记、内容（前 50 字）、是否已转任务 | 最多 8 条 |
| 语言转换记录 | 消极原话 → 积极改写（各前 24 字） | 最多 3 条 |
| 周计划 | 本周大石头标题与理由 | — |
| 平衡评分 | 产出/产能分数与诊断语 | — |
| 用户画像与长期记忆 | `user_profile` 中的基本设定 + LLM 从历次对话沉淀的「关于你的事实」（按置信度取前 15 条） | 前 15 条 |
| 管家自定义约束 | 你让管家记住的规则（≤30 条） | 30 条 |
| 最近对话 | 你的消息原文与管家回复 | 最近约 12 轮 |

**健康原始值不在其中**：常规管家上下文不包含睡眠时长、心率、步数、HRV 或健康备注；健康数据只在你主动打开健康顾问并发起解读时进入该次请求。

## 三、健康顾问场景（仅当你主动打开）

在健康页主动点击「随行顾问」时，额外发送**健康摘要**（非原始逐条数据）：近 7–14 天的睡眠/HRV/锻炼聚合趋势、昨日对比、规则引擎的异常提示。

## 四、图片理解场景（仅当你主动发图）

管家「看图」功能仅在你已配置 `LLM_API_BASE` 和 `LLM_API_KEY`、且未处于演示模式时，把**你发送的图片本体**发给你配置的模型服务；未配置或演示模式会在本机拒绝，不会外发。请求附带的系统提示词不含个人画像、自定义规则或健康数据。

## 五、哪些情况不会发送

- 未配置 `LLM_API_KEY`（规则模式，零外发）
- 仅浏览/编辑/统计，不打开 AI 对话
- 神谕/周报等后台生成**只在配置了 LLM 时**运行；未配置时返回占位文案
- 演示模式（`DASH_DEMO=1`）：AI 明确提示未配置，不外发演示数据

## 六、如何彻底关闭 AI

删除或留空配置中的 `LLM_API_KEY`（与 `LLM_API_BASE`）即可，无需改代码。管家仍提供基于规则的引导。

## 七、数据落盘位置

- 对话历史/画像/记忆：`data/` 目录（`DASH_DATA_DIR` 可改）
- 不写浏览器之外的任何位置；无遥测、无上报、无开发者服务器

## 八、变更

本文件的任何实质变更会随 release note 公布。发现描述与代码不符请提 issue——以代码行为为准，我们会修文档或修代码。
